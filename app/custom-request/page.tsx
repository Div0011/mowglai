import CustomRequestClient from "./CustomRequestClient";

export const metadata = {
    title: "Request Custom Quote | Mowglai",
    description: "Tell us about your project vision. Get a tailored quotation for your custom web development needs.",
};

export default function CustomRequest() {
    return <CustomRequestClient />;
}
