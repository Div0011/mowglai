import ProjectRequestClient from "./ProjectRequestClient";

export const metadata = {
    title: "Project Request | Mowglai",
    description: "Start your project with Mowglai. Select your plan and tell us about your vision. We're ready to build something extraordinary.",
};

export default function ProjectRequest() {
    return <ProjectRequestClient />;
}
